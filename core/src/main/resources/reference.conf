source {
  type = mysql
  mysql {
    host = "localhost"
    port = 8888
    url = "jdbc:mysql://localhost:8888/scm?useSSL=false&allowPublicKeyRetrieval=true"
    username = "root"
    password = "Surya@1234"
    schema = "scm"
    tables = "all"
  }
}

target {
  type = redshift
  redshift {

  }
}